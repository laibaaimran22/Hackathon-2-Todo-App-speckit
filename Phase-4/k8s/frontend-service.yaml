apiVersion: v1
kind: Service
metadata:
  name: frontend-service
  labels:
    app: frontend
spec:
  type: NodePort  # For Minikube access
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080  # Specific port for consistency
    name: http
  selector:
    app: frontend