apiVersion: v1
kind: Service
metadata:
  name: backend-service
  labels:
    app: backend
spec:
  type: ClusterIP  # Internal service only
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  selector:
    app: backend